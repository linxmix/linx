/* jshint expr:true */
import { expect } from 'chai';
import {
  describe,
  it
} from 'mocha';
import {
  svgTransition
} from 'linx/helpers/svg-transition';

describe('SvgTransitionHelper', function() {
  // Replace this with your real tests.
  it('works', function() {
    var result = svgTransition(42);
    expect(result).to.be.ok;
  });
});
