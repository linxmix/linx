{{#if showBeatGrid}}
  {{#data-visual
    as |svg width height|
  }}
    {{d3-axis
      select=(svg-transition svg.beat-grid)
      transform=(svg-translate 0 125)
      scale=beatScale
      orient="top"
      tickSize=125
      ticks=numBeats
    }}
  {{/data-visual}}
{{/if}}