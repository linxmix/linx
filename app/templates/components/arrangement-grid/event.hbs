{{#component clip.componentName
  clip=clip
  clipEvent=clipEvent
  sinkNode=sinkNode
  pxPerBeat=pxPerBeat
  as |clipComponent|
}}
  {{#each clipComponent.visibleMarkers as |marker|}}
    {{clip-marker
      marker=marker
      pxPerBeat=pxPerBeat
    }}
  {{/each}}
{{/component}}
