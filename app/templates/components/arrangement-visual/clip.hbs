{{yield}}

{{arrangement-visual/clip/resize-handle
  select=select.ArrangementVisualClip-leftResizeHandle
  direction="left"
  isResizable=isResizable
  height=height
  widthPx=widthPx
  pxPerBeat=pxPerBeat
  onDragStart=(action "onResizeStart")
  onDrag=(action "onResizeLeft")
  dragTransformsParent=true
}}

{{arrangement-visual/clip/resize-handle
  select=select.ArrangementVisualClip-rightResizeHandle
  direction="right"
  isResizable=isResizable
  height=height
  widthPx=widthPx
  pxPerBeat=pxPerBeat
  onDragStart=(action "onResizeStart")
  onDrag=(action "onResizeRight")
  dragTransformsParent=true
}}