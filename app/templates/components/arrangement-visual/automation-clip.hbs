{{#if _drawControlPoints}}
  {{arrangement-visual/automation-clip/control-points
    select=select.ArrangementVisualAutomationClip-ControlPoints
    controlPoints=controlPoints
    height=height
    pxPerBeat=pxPerBeat
    isDraggable=true
    onDrag=(action "onControlPointDrag")
    onDragStart=(action "onControlPointDragStart")
    onDragEnd=(action "onControlPointDragEnd")
  }}
{{/if}}

{{yield}}