{{arrangement-player arrangement=arrangement}}

{{#each row in arrangement.rows}}
  {{#arrangement-row model=row}}
    {{#each clip in row.clips}}
      {{arrangement-clip model=clip}}
    {{/each}}
  {{/arrangement-row}}
{{/each}}
